def SelectionSort( arr ):
	size=len(arr)
	for i in range(0, size):
		minim = arr[i]
		minimPos = i
		for j in range(i, size):
			if arr[j] < minim:
				minim = arr[j]
				minimPos = j
		arr[minimPos] = arr[i]
		arr[i] = minim
arr=[7,5,9,6,8,2,3,1,4]
SelectionSort(arr)
print arr
